class_name FactionGameData extends Resource

# (({[%%%(({[=======================================================================================================================]}))%%%]}))
@export var faction_id: int
@export var starting_bread_count: int

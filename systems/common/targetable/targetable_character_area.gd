class_name TargetableCharacterArea extends Area3D

# (({[%%%(({[=======================================================================================================================]}))%%%]}))
# This area just guarantees that its parent is a Character
@export var team: int
